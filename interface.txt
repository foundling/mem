CLI 

'task'
shows all tasks or prints 'no tasks'

:: select name, description, complete from tasks

name:
description:
complete:

'task -a, --add'
prompts with menu 

:: insert into tasks values (NULL, 'task', 'task description', date('now'), 0)  

'task --help'
help, option menu

'task -d, --delete'

select id, name from tasks


